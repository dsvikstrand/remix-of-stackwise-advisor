# Oracle Deploy Guide (Agentic Backend)

## Goal
Keep the Oracle server running the latest `main` branch with minimal downtime.

## Steps (repeatable)
- [ ] SSH into the server.
- [ ] `cd /home/ubuntu/remix-of-stackwise-advisor`
- [ ] `git status -sb` (confirm branch = `main`)
- [ ] `git pull`
- [ ] If `package-lock.json` changed → `npm ci --omit=dev --no-audit --no-fund`
- [ ] `sudo systemctl restart agentic-backend`
- [ ] `curl -i http://localhost:8787/api/health` (expect 200)

## Notes
- Use `sudo journalctl -u agentic-backend -n 50 --no-pager` for errors.
- Route check for new endpoints:
  `curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8787/api/generate-banner`
  (Expect 401 if auth is required.)
